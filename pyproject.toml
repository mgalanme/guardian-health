[project]
name = "guardian-health"
version = "0.1.0"
description = "Governance and Accountability by Design in Agentic AI — Healthcare Case Study"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py311"
